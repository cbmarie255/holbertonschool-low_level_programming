#include "main.h"
void print_alphabet(void);
/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
for (int aplha = 'a'; alpha <= 'z'; alpha++)
{
int alpha = c;
_putchar(c);
print_alphabet(c);
}
_putchar('\n');
return (0);
}
