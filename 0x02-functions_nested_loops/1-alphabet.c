#include "main.h"

/**
 * print_alphabet - print alphabet
 *
 * Return: Always 0.
 */
void print_alphabet(void);
int alpha = 0;
{
for (alpha = 'a'; alpha <= 'z'; alpha++)
{
_putchar(alpha);
}
_putchar('\n');
return (0);
}
