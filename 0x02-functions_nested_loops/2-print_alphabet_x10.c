#include "main.h"
void print_alphabet_x10(void);
/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int main(void)
{
while (int alpha = 'a'; alpha<= 'z'; alpha++)
{
_putschar(alpha);
_putchar('\n');
num = 0;
while (num = 0)
{
print_alphabet_x10(alpha);
num++;
}
return (0);
}
}
