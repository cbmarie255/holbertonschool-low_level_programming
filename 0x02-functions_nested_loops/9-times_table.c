#include "main.h"
/**
 *times_table - will print an oldschool times table
 *
 * Return: Always 0
 */
void times_table(void)
{
int c = 0;
int r = 0;
int p = 0;

for (c = 0; c < 9; c++ && r = 0; r < 9; r++)
{
while ((r * c = p))
{
_putchar((p / 10) + '0');
_putchar((p % 10) + '0');
_putchar(',');
_putchar(' ');
_putchar('\n');
}
}
}
