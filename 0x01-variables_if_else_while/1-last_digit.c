#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main- This script will display the last digit (n) followed by more instructions
 *
 * Return: Always 0 (successful)
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	
	return (0);
}
