#include "main.h"
/**
 * _isupper - checks for an uppercase letter
 *@c: character used to check for uppercase letter
 * Return: 0 or 1
 */
int _isupper(int c)
{
if (_isupper(c))
{
return (1);
}
else 
{
return (0);
}
}
