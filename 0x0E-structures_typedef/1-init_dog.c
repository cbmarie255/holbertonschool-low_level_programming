#include "dog.h"
#include <stdlib.h>
#include <stdio.h>
/**
 *@d: pointer used for user
 *@name: name of dog
 *@age: age of dog
 *@owner: owner of dog
 */
void init_dog(struct dog *d, char *name, float age, char *owner)
{
	if (d == NULL)
	{
		return (NULL);
	}

	d -> name;
	d -> age;
	d -> owner;
}
